# 🎴 Poker Bot — Texas Hold'em برای تلگرام

یک بات تلگرام برای بازی **پوکر نو-لیمیت تگزاس هولدم** به صورت چند نفره داخل گروه‌های تلگرام. کارت‌های هر بازیکن به صورت خصوصی (DM) ارسال می‌شود و شرط‌بندی با دکمه‌های شیشه‌ای (inline) داخل گروه انجام می‌گیرد.

## امکانات

- ✅ بازی کامل تگزاس هولدم: پری‌فلاپ، فلاپ، ترن، ریور، شوداون
- ✅ مدیریت بلایند کوچک/بزرگ و دکمه دیلر
- ✅ اکشن‌های فولد / چک / کال / بت / ریز / آل‌این با دکمه
- ✅ محاسبه‌ی **side pot** برای حالت‌های آل‌این
- ✅ ارزیابی دقیق دست‌ها (از رویال فلاش تا های‌کارت، شامل straight چرخ A-2-3-4-5)
- ✅ کارت‌ها به صورت خصوصی برای هر بازیکن ارسال می‌شود
- ✅ پشتیبانی از بازی دو نفره (heads-up)

## راه‌اندازی (لایو روی تلگرام)

### ۱) ساخت بات و گرفتن توکن
1. در تلگرام به [@BotFather](https://t.me/BotFather) پیام بدهید.
2. دستور `/newbot` را بزنید و نام و یوزرنیم انتخاب کنید.
3. توکنی که می‌دهد را کپی کنید.
4. برای اینکه بات بتواند همه پیام‌ها/دستورها را در گروه ببیند، در BotFather:
   `/setprivacy` → بات خود را انتخاب کنید → **Disable**.

### ۲) نصب و اجرا

```bash
pip install -r requirements.txt
export BOT_TOKEN="توکنی که از BotFather گرفتید"
python bot.py
```

بات با long polling اجرا می‌شود؛ تا وقتی این پروسه روشن باشد، بات لایو است.

### ۳) شروع بازی در تلگرام
1. ابتدا هر بازیکن باید در **چت خصوصی** بات `/start` را بزند (تا بات بتواند کارت‌ها را خصوصی بفرستد).
2. بات را به یک گروه اضافه کنید.
3. در گروه:
   - `/newtable` — ساخت میز (یا `/newtable 25 50` برای تعیین بلایند)
   - `/join` — هر بازیکن این را می‌زند
   - `/begin` — شروع دست و پخش کارت‌ها
   - شرط‌بندی با دکمه‌ها انجام می‌شود
   - بعد از هر دست دوباره `/begin`

## دیپلوی دائمی (۲۴ ساعته لایو)

برای اینکه بات همیشه روشن بماند، آن را روی یک هاست اجرا کنید (نه روی کامپیوتر شخصی که خاموش می‌شود).

### گزینه‌ی Docker
```bash
docker build -t poker-bot .
docker run -d --restart=always -e BOT_TOKEN="توکن_شما" --name poker-bot poker-bot
```

### Railway / Render / Fly.io و مشابه
1. این ریپو را به سرویس وصل کنید.
2. متغیر محیطی `BOT_TOKEN` را در تنظیمات سرویس قرار دهید.
3. نوع پروسه را **worker** انتخاب کنید (فایل `Procfile` آماده است). این بات سرور وب نیست و پورت باز نمی‌کند.

### یک سرور لینوکسی (VPS)
```bash
git clone <repo>
cd poker-bot
pip install -r requirements.txt
BOT_TOKEN="توکن_شما" nohup python bot.py &
```

> توجه: محیط Claude Code on the web به دلیل network policy به `api.telegram.org` دسترسی ندارد، بنابراین بات از داخل آن لایو نمی‌شود — باید جایی اجرا شود که به تلگرام دسترسی دارد.

## دستورات

| دستور | توضیح |
|-------|-------|
| `/start` | معرفی و راهنما |
| `/newtable [sb bb]` | ساخت میز جدید |
| `/join` | نشستن سر میز |
| `/leave` | ترک میز |
| `/begin` | شروع دست |
| `/table` | وضعیت میز و موجودی‌ها |
| `/cards` | ارسال مجدد کارت‌های شما (خصوصی) |
| `/chips` | موجودی ژتون شما |

## معماری

```
poker/
  cards.py       # کارت و دسته کارت
  evaluator.py   # ارزیابی بهترین دست ۵ کارتی از ۷ کارت
  game.py        # موتور بازی: state machine، شرط‌بندی، side pot، showdown
bot.py           # لایه تلگرام (هندلرها و دکمه‌ها)
tests/
  test_engine.py # تست‌های موتور بازی
```

موتور بازی (`poker/`) کاملاً مستقل از تلگرام است و جداگانه قابل تست است.

## اجرای تست‌ها

```bash
python tests/test_engine.py
```

## نکات

- ژتون‌ها صرفاً «play money» هستند و ارزش واقعی ندارند.
- داده‌ها در حافظه نگه‌داری می‌شوند؛ با ری‌استارت بات میزها پاک می‌شوند.
